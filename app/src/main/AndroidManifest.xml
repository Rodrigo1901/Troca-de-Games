<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.res.trocadejogos">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.Mapa"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Views.MapaFragmento"
            android:label="@string/title_activity_mapa" />
        <activity
            android:name=".Views.EditarJogo"
            android:parentActivityName=".Views.Biblioteca" />
        <activity android:name=".Views.Conversas" />
        <activity
            android:name=".Views.RedefinirSenha"
            android:parentActivityName=".Views.MainActivity" />
        <activity
            android:name=".Views.AdicionarJogo"
            android:parentActivityName=".Views.Biblioteca" />
        <activity
            android:name=".Views.Chat"
            android:parentActivityName=".Views.Conversas"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.res.trocadejogos.Views.Biblioteca" />
        </activity>
        <activity
            android:name=".Views.Perfil"
            android:parentActivityName=".Views.Biblioteca" />
        <activity android:name=".Views.Biblioteca" />
        <activity
            android:name=".Views.CadastroUsuario"
            android:parentActivityName=".Views.MainActivity" />
        <activity android:name=".Views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>